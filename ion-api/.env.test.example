# Copy this file to .env.test and fill in a dedicated test database URI.
# Tests will use MONGO_URI_TEST when NODE_ENV=test.
# Use a separate database from your dev DB so test runs can safely wipe data.

MONGO_URI_TEST=mongodb://localhost:27017/luren_test
JWT_SECRET=test_secret_change_me
